import { theme } from "@/styledComponents";
import styled from "styled-components";

export const DivCardNoEvent = styled.div`
  pointer-events: none;
`;

export const DivClientCardMiProfile = styled.div`
  text-decoration: none;
  color: ${theme.textColor};
`;
