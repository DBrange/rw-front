import { Loader } from "..";
import { SectionGlobalLoader } from "./GlobalLoader.styled";

function GlobalLoader() {
  return (
    <SectionGlobalLoader>
      <Loader />
    </SectionGlobalLoader>
  );
}
export default GlobalLoader;
