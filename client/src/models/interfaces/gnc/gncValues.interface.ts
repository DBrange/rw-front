export interface GncValues{
  oblea: string;
  plate: string;
  expireDate: string;
}

export interface ErrorsGncValues {
  oblea: string;
  plate: string;
  expireDate: string;
}

export interface TouchedGncValues {
  oblea: boolean;
  plate: boolean;
  expireDate: boolean;
}